<div class="btn-group">
        <button type="button" onclick="deleteData('{{ $data->id }}')" title="{{ $data->nama }}" class="delete btn btn-danger btn-sm"><i class="fa fa-trash-o text-danger"></i>Cancel</button>
    </ul>
</div>